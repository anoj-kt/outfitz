import { UserIcon, UserIconContainer } from './user-icon.styles';

const User = () => {
    

    return (
        <UserIconContainer>
            <UserIcon />
        </UserIconContainer>
    )
};

export default User; 